import React from 'react';

// This component is unused and was causing build errors.
// Its content has been removed to resolve them as file deletion is not supported.
export const DecisionPanel: React.FC<{ onComplete: () => void }> = () => {
  return null;
};
